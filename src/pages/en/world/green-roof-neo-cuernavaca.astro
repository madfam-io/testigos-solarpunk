---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import { useTranslations } from '@/i18n/config';

const lang = 'en';
const t = useTranslations(lang);
let basePath =
  import.meta.env.BASE_URL != null && import.meta.env.BASE_URL !== ''
    ? import.meta.env.BASE_URL
    : '/testigos-solarpunk';

// Ensure basePath doesn't end with a slash
if (basePath.endsWith('/')) {
  basePath = basePath.slice(0, -1);
}
---

<BaseLayout
  lang={lang}
  title={t('greenroof.title')}
  description={t('greenroof.description')}
>
  <!-- Breadcrumb Navigation -->
  <nav class="breadcrumb-nav" aria-label="Breadcrumb navigation">
    <div class="container">
      <ol class="breadcrumb">
        <li>
          <a href={`${basePath}/en/`}>{t('greenroof.breadcrumb.home')}</a>
        </li>
        <li>
          <a href={`${basePath}/en/world/`}
            >{t('greenroof.breadcrumb.world')}</a>
        </li>
        <li aria-current="page">{t('greenroof.title')}</li>
      </ol>
    </div>
  </nav>

  <!-- Hero Section -->
  <section class="location-hero">
    <div class="container">
      <div class="hero-content">
        <span class="emoji-cutout emoji-hero emoji-xl theme-green flutter"
          >🌿</span
        >
        <h1>{t('greenroof.hero.title')}</h1>
        <p class="hero-description">
          {t('greenroof.hero.description')}
        </p>
      </div>
    </div>
  </section>

  <!-- Overview Section -->
  <section class="overview-section">
    <div class="container">
      <div class="overview-content">
        <div class="overview-text">
          <h2>
            <span class="emoji-cutout emoji-md theme-solar">☀️</span>
            {t('greenroof.overview.title')}
          </h2>
          <p>
            {t('greenroof.overview.p1')}
          </p>
          <p>
            {t('greenroof.overview.p2')}
          </p>
        </div>
        <div class="overview-stats">
          <div class="stat-card">
            <span class="stat-number">2.5K</span>
            <span class="stat-label">{t('greenroof.stats.area')}</span>
          </div>
          <div class="stat-card">
            <span class="stat-number">500+</span>
            <span class="stat-label">{t('greenroof.stats.plants')}</span>
          </div>
          <div class="stat-card">
            <span class="stat-number">85%</span>
            <span class="stat-label">{t('greenroof.stats.renewable')}</span>
          </div>
          <div class="stat-card">
            <span class="stat-number">200</span>
            <span class="stat-label">{t('greenroof.stats.visitors')}</span>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Features Section -->
  <section class="features-section">
    <div class="container">
      <h2>
        <span class="emoji-cutout emoji-md theme-purple">🏗️</span>
        {t('greenroof.features.title')}
      </h2>
      <div class="features-grid">
        <div class="feature-card card">
          <div class="feature-header">
            <span class="emoji-cutout emoji-lg theme-solar">⚡</span>
            <h3>{t('greenroof.features.solar.title')}</h3>
          </div>
          <div class="feature-content">
            <p>
              {t('greenroof.features.solar.description')}
            </p>
            <ul>
              <li>{t('greenroof.features.solar.feature1')}</li>
              <li>{t('greenroof.features.solar.feature2')}</li>
              <li>{t('greenroof.features.solar.feature3')}</li>
              <li>{t('greenroof.features.solar.feature4')}</li>
            </ul>
          </div>
        </div>

        <div class="feature-card card">
          <div class="feature-header">
            <span class="emoji-cutout emoji-lg theme-green">🌱</span>
            <h3>{t('greenroof.features.gardens.title')}</h3>
          </div>
          <div class="feature-content">
            <p>
              {t('greenroof.features.gardens.description')}
            </p>
            <ul>
              <li>{t('greenroof.features.gardens.feature1')}</li>
              <li>{t('greenroof.features.gardens.feature2')}</li>
              <li>{t('greenroof.features.gardens.feature3')}</li>
              <li>{t('greenroof.features.gardens.feature4')}</li>
            </ul>
          </div>
        </div>

        <div class="feature-card card">
          <div class="feature-header">
            <span class="emoji-cutout emoji-lg theme-community">🏛️</span>
            <h3>{t('greenroof.features.meeting.title')}</h3>
          </div>
          <div class="feature-content">
            <p>
              {t('greenroof.features.meeting.description')}
            </p>
            <ul>
              <li>{t('greenroof.features.meeting.feature1')}</li>
              <li>{t('greenroof.features.meeting.feature2')}</li>
              <li>{t('greenroof.features.meeting.feature3')}</li>
              <li>{t('greenroof.features.meeting.feature4')}</li>
            </ul>
          </div>
        </div>

        <div class="feature-card card">
          <div class="feature-header">
            <span class="emoji-cutout emoji-lg theme-purple">♻️</span>
            <h3>{t('greenroof.features.compost.title')}</h3>
          </div>
          <div class="feature-content">
            <p>
              {t('greenroof.features.compost.description')}
            </p>
            <ul>
              <li>{t('greenroof.features.compost.feature1')}</li>
              <li>{t('greenroof.features.compost.feature2')}</li>
              <li>{t('greenroof.features.compost.feature3')}</li>
              <li>{t('greenroof.features.compost.feature4')}</li>
            </ul>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Daily Life Section -->
  <section class="daily-life-section">
    <div class="container">
      <h2>
        <span class="emoji-cutout emoji-md theme-solar">🌅</span>
        {t('greenroof.dailylife.title')}
      </h2>
      <div class="timeline">
        <div class="timeline-item">
          <div class="time-marker">6:00 AM</div>
          <div class="timeline-content">
            <h3>{t('greenroof.dailylife.6am.title')}</h3>
            <p>
              {t('greenroof.dailylife.6am.description')}
            </p>
          </div>
        </div>

        <div class="timeline-item">
          <div class="time-marker">8:00 AM</div>
          <div class="timeline-content">
            <h3>{t('greenroof.dailylife.8am.title')}</h3>
            <p>
              {t('greenroof.dailylife.8am.description')}
            </p>
          </div>
        </div>

        <div class="timeline-item">
          <div class="time-marker">12:00 PM</div>
          <div class="timeline-content">
            <h3>{t('greenroof.dailylife.12pm.title')}</h3>
            <p>
              {t('greenroof.dailylife.12pm.description')}
            </p>
          </div>
        </div>

        <div class="timeline-item">
          <div class="time-marker">4:00 PM</div>
          <div class="timeline-content">
            <h3>{t('greenroof.dailylife.4pm.title')}</h3>
            <p>
              {t('greenroof.dailylife.4pm.description')}
            </p>
          </div>
        </div>

        <div class="timeline-item">
          <div class="time-marker">6:00 PM</div>
          <div class="timeline-content">
            <h3>{t('greenroof.dailylife.6pm.title')}</h3>
            <p>
              {t('greenroof.dailylife.6pm.description')}
            </p>
          </div>
        </div>

        <div class="timeline-item">
          <div class="time-marker">8:00 PM</div>
          <div class="timeline-content">
            <h3>{t('greenroof.dailylife.8pm.title')}</h3>
            <p>
              {t('greenroof.dailylife.8pm.description')}
            </p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Impact Section -->
  <section class="impact-section">
    <div class="container">
      <h2>
        <span class="emoji-cutout emoji-md theme-green">📊</span>
        {t('greenroof.impact.title')}
      </h2>
      <div class="impact-grid">
        <div class="impact-card">
          <div class="impact-number">{t('greenroof.impact.energy.number')}</div>
          <div class="impact-label">{t('greenroof.impact.energy.label')}</div>
          <p>
            {t('greenroof.impact.energy.description')}
          </p>
        </div>

        <div class="impact-card">
          <div class="impact-number">{t('greenroof.impact.co2.number')}</div>
          <div class="impact-label">{t('greenroof.impact.co2.label')}</div>
          <p>
            {t('greenroof.impact.co2.description')}
          </p>
        </div>

        <div class="impact-card">
          <div class="impact-number">{t('greenroof.impact.waste.number')}</div>
          <div class="impact-label">{t('greenroof.impact.waste.label')}</div>
          <p>
            {t('greenroof.impact.waste.description')}
          </p>
        </div>

        <div class="impact-card">
          <div class="impact-number">{t('greenroof.impact.food.number')}</div>
          <div class="impact-label">{t('greenroof.impact.food.label')}</div>
          <p>
            {t('greenroof.impact.food.description')}
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- Stories Section -->
  <section class="stories-section">
    <div class="container">
      <h2>
        <span class="emoji-cutout emoji-md theme-purple">📖</span>
        {t('greenroof.stories.title')}
      </h2>
      <div class="stories-grid">
        <div class="story-card card">
          <div class="story-header">
            <span class="emoji-cutout emoji-lg theme-community">👩‍👧‍👦</span>
            <h3>{t('greenroof.stories.hernandez.title')}</h3>
          </div>
          <div class="story-content">
            <p>
              {t('greenroof.stories.hernandez.quote')}
            </p>
            <div class="story-impact">
              <strong>Impact:</strong>
              {t('greenroof.stories.hernandez.impact')}
            </div>
          </div>
        </div>

        <div class="story-card card">
          <div class="story-header">
            <span class="emoji-cutout emoji-lg theme-green">👨‍🌾</span>
            <h3>{t('greenroof.stories.carlos.title')}</h3>
          </div>
          <div class="story-content">
            <p>
              {t('greenroof.stories.carlos.quote')}
            </p>
            <div class="story-impact">
              <strong>Impact:</strong>
              {t('greenroof.stories.carlos.impact')}
            </div>
          </div>
        </div>

        <div class="story-card card">
          <div class="story-header">
            <span class="emoji-cutout emoji-lg theme-solar">👧</span>
            <h3>{t('greenroof.stories.sofia.title')}</h3>
          </div>
          <div class="story-content">
            <p>
              {t('greenroof.stories.sofia.quote')}
            </p>
            <div class="story-impact">
              <strong>Impact:</strong>
              {t('greenroof.stories.sofia.impact')}
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Future Vision Section -->
  <section class="future-section">
    <div class="container">
      <div class="future-content">
        <h2>
          <span class="emoji-cutout emoji-md theme-solar">🚀</span>
          {t('greenroof.future.title')}
        </h2>
        <p class="future-description">
          {t('greenroof.future.description')}
        </p>
        <div class="future-goals">
          <div class="goal-item">
            <span class="emoji-cutout emoji-md theme-green">🌱</span>
            <h3>{t('greenroof.future.goal1.title')}</h3>
            <p>{t('greenroof.future.goal1.description')}</p>
          </div>
          <div class="goal-item">
            <span class="emoji-cutout emoji-md theme-solar">⚡</span>
            <h3>{t('greenroof.future.goal2.title')}</h3>
            <p>{t('greenroof.future.goal2.description')}</p>
          </div>
          <div class="goal-item">
            <span class="emoji-cutout emoji-md theme-community">🤝</span>
            <h3>{t('greenroof.future.goal3.title')}</h3>
            <p>{t('greenroof.future.goal3.description')}</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Navigation -->
  <section class="location-navigation">
    <div class="container">
      <div class="nav-buttons">
        <a href={`${basePath}/en/world/`} class="btn btn-secondary">
          <span class="emoji-cutout emoji-sm">🌍</span>
          {t('greenroof.nav.back')}
        </a>
        <a href={`${basePath}/en/characters/`} class="btn btn-primary">
          <span class="emoji-cutout emoji-sm">👥</span>
          {t('greenroof.nav.characters')}
        </a>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  .breadcrumb-nav {
    background-color: var(--fondo-secundario);
    padding: var(--espacio-md) 0;
    border-bottom: 1px solid var(--borde-claro);
  }

  .breadcrumb {
    display: flex;
    list-style: none;
    margin: 0;
    padding: 0;
    font-size: 0.875rem;
  }

  .breadcrumb li {
    color: var(--texto-secundario);
  }

  .breadcrumb li:not(:last-child)::after {
    content: ' / ';
    margin: 0 var(--espacio-sm);
    color: var(--texto-terciario);
  }

  .breadcrumb a {
    color: var(--azul-tecnologico);
    text-decoration: none;
  }

  .breadcrumb a:hover {
    text-decoration: underline;
  }

  .location-hero {
    padding: var(--espacio-xxl) 0;
    background: linear-gradient(
      135deg,
      var(--verde-sostenible) 0%,
      rgba(76, 175, 80, 0.1) 100%
    );
    text-align: center;
    position: relative;
    overflow: hidden;
  }

  .location-hero::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-image: repeating-linear-gradient(
      45deg,
      transparent,
      transparent 10px,
      rgba(255, 255, 255, 0.05) 10px,
      rgba(255, 255, 255, 0.05) 20px
    );
    pointer-events: none;
  }

  .hero-content h1 {
    font-size: 2.5rem;
    margin: var(--espacio-lg) 0;
    color: var(--texto-primario);
  }

  .hero-description {
    font-size: 1.2rem;
    color: var(--texto-secundario);
    max-width: 700px;
    margin: 0 auto;
    line-height: 1.6;
  }

  .overview-section {
    padding: var(--espacio-xxl) 0;
  }

  .overview-content {
    display: grid;
    grid-template-columns: 2fr 1fr;
    gap: var(--espacio-xxl);
    align-items: start;
  }

  .overview-text h2 {
    display: flex;
    align-items: center;
    gap: var(--espacio-md);
    margin-bottom: var(--espacio-lg);
    color: var(--amarillo-solar);
  }

  .overview-text p {
    color: var(--texto-secundario);
    line-height: 1.6;
    margin-bottom: var(--espacio-lg);
  }

  .overview-stats {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--espacio-md);
  }

  .stat-card {
    background: var(--fondo-secundario);
    padding: var(--espacio-lg);
    border-radius: 8px;
    text-align: center;
    border: 2px solid var(--borde-claro);
  }

  .stat-number {
    display: block;
    font-size: 1.8rem;
    font-weight: 700;
    color: var(--verde-sostenible);
    margin-bottom: var(--espacio-xs);
  }

  .stat-label {
    color: var(--texto-secundario);
    font-size: 0.9rem;
  }

  .features-section {
    padding: var(--espacio-xxl) 0;
    background-color: var(--fondo-secundario);
  }

  .features-section h2 {
    display: flex;
    align-items: center;
    gap: var(--espacio-md);
    justify-content: center;
    margin-bottom: var(--espacio-xl);
    color: var(--amarillo-solar);
  }

  .features-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
    gap: var(--espacio-lg);
    margin-top: var(--espacio-xl);
  }

  .feature-card {
    padding: var(--espacio-xl);
  }

  .feature-header {
    display: flex;
    align-items: center;
    gap: var(--espacio-md);
    margin-bottom: var(--espacio-lg);
    padding-bottom: var(--espacio-md);
    border-bottom: 2px solid var(--borde-claro);
  }

  .feature-header h3 {
    color: var(--verde-sostenible);
    margin: 0;
    font-size: 1.2rem;
  }

  .feature-content p {
    color: var(--texto-secundario);
    margin-bottom: var(--espacio-lg);
    line-height: 1.6;
  }

  .feature-content ul {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .feature-content li {
    padding: var(--espacio-xs) 0;
    border-bottom: 1px solid rgba(255, 255, 255, 0.05);
    color: var(--texto-secundario);
    position: relative;
    padding-left: var(--espacio-lg);
  }

  .feature-content li:before {
    content: '✓';
    position: absolute;
    left: 0;
    color: var(--verde-sostenible);
    font-weight: bold;
  }

  .feature-content li:last-child {
    border-bottom: none;
  }

  .daily-life-section {
    padding: var(--espacio-xxl) 0;
  }

  .daily-life-section h2 {
    display: flex;
    align-items: center;
    gap: var(--espacio-md);
    justify-content: center;
    margin-bottom: var(--espacio-xl);
    color: var(--amarillo-solar);
  }

  .timeline {
    position: relative;
    max-width: 800px;
    margin: var(--espacio-xl) auto 0;
  }

  .timeline::before {
    content: '';
    position: absolute;
    left: 80px;
    top: 0;
    bottom: 0;
    width: 2px;
    background: var(--verde-sostenible);
  }

  .timeline-item {
    position: relative;
    margin-bottom: var(--espacio-xl);
    padding-left: 140px;
  }

  .time-marker {
    position: absolute;
    left: 0;
    top: 0;
    width: 120px;
    padding: var(--espacio-sm);
    background: var(--amarillo-solar);
    color: var(--negro-profundo);
    border-radius: 20px;
    text-align: center;
    font-weight: 600;
    font-size: 0.9rem;
  }

  .timeline-content {
    background: var(--fondo-secundario);
    padding: var(--espacio-lg);
    border-radius: 8px;
    border: 2px solid var(--borde-claro);
  }

  .timeline-content h3 {
    color: var(--verde-sostenible);
    margin: 0 0 var(--espacio-md) 0;
  }

  .timeline-content p {
    color: var(--texto-secundario);
    margin: 0;
    line-height: 1.6;
  }

  .impact-section {
    padding: var(--espacio-xxl) 0;
    background-color: var(--fondo-secundario);
  }

  .impact-section h2 {
    display: flex;
    align-items: center;
    gap: var(--espacio-md);
    justify-content: center;
    margin-bottom: var(--espacio-xl);
    color: var(--amarillo-solar);
  }

  .impact-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: var(--espacio-lg);
    margin-top: var(--espacio-xl);
  }

  .impact-card {
    background: var(--fondo-primario);
    padding: var(--espacio-xl);
    border-radius: 8px;
    border: 2px solid var(--borde-claro);
    text-align: center;
  }

  .impact-number {
    font-size: 2.5rem;
    font-weight: 700;
    color: var(--verde-sostenible);
    display: block;
    margin-bottom: var(--espacio-sm);
  }

  .impact-label {
    color: var(--amarillo-solar);
    font-weight: 600;
    margin-bottom: var(--espacio-md);
    display: block;
  }

  .impact-card p {
    color: var(--texto-secundario);
    margin: 0;
    line-height: 1.5;
    font-size: 0.9rem;
  }

  .stories-section {
    padding: var(--espacio-xxl) 0;
  }

  .stories-section h2 {
    display: flex;
    align-items: center;
    gap: var(--espacio-md);
    justify-content: center;
    margin-bottom: var(--espacio-xl);
    color: var(--amarillo-solar);
  }

  .stories-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
    gap: var(--espacio-lg);
    margin-top: var(--espacio-xl);
  }

  .story-card {
    padding: var(--espacio-xl);
  }

  .story-header {
    display: flex;
    align-items: center;
    gap: var(--espacio-md);
    margin-bottom: var(--espacio-lg);
    padding-bottom: var(--espacio-md);
    border-bottom: 2px solid var(--borde-claro);
  }

  .story-header h3 {
    color: var(--verde-sostenible);
    margin: 0;
    font-size: 1.1rem;
  }

  .story-content p {
    color: var(--texto-secundario);
    line-height: 1.6;
    margin-bottom: var(--espacio-lg);
    font-style: italic;
  }

  .story-impact {
    color: var(--amarillo-solar);
    font-size: 0.9rem;
    padding: var(--espacio-sm);
    background: rgba(255, 193, 7, 0.1);
    border-radius: 6px;
  }

  .future-section {
    padding: var(--espacio-xxl) 0;
    background-color: var(--fondo-secundario);
    text-align: center;
  }

  .future-content h2 {
    display: flex;
    align-items: center;
    gap: var(--espacio-md);
    justify-content: center;
    margin-bottom: var(--espacio-lg);
    color: var(--amarillo-solar);
  }

  .future-description {
    color: var(--texto-secundario);
    max-width: 700px;
    margin: 0 auto var(--espacio-xxl);
    line-height: 1.6;
    font-size: 1.1rem;
  }

  .future-goals {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: var(--espacio-lg);
    margin-top: var(--espacio-xl);
  }

  .goal-item {
    padding: var(--espacio-lg);
    background: var(--fondo-primario);
    border-radius: 8px;
    border: 2px solid var(--borde-claro);
  }

  .goal-item h3 {
    color: var(--verde-sostenible);
    margin: var(--espacio-md) 0;
  }

  .goal-item p {
    color: var(--texto-secundario);
    margin: 0;
    font-size: 0.9rem;
  }

  .location-navigation {
    padding: var(--espacio-xxl) 0;
  }

  .nav-buttons {
    display: flex;
    justify-content: space-between;
    gap: var(--espacio-lg);
  }

  /* Mobile Responsiveness */
  @media (max-width: 768px) {
    .hero-content h1 {
      font-size: 2rem;
    }

    .overview-content {
      grid-template-columns: 1fr;
      gap: var(--espacio-lg);
    }

    .overview-stats {
      grid-template-columns: 1fr 1fr;
    }

    .features-grid,
    .stories-grid {
      grid-template-columns: 1fr;
    }

    .impact-grid {
      grid-template-columns: repeat(2, 1fr);
    }

    .future-goals {
      grid-template-columns: 1fr;
    }

    .timeline::before {
      left: 20px;
    }

    .timeline-item {
      padding-left: 60px;
    }

    .time-marker {
      width: 80px;
      font-size: 0.8rem;
      padding: var(--espacio-xs);
    }

    .nav-buttons {
      flex-direction: column;
      align-items: center;
    }

    .nav-buttons .btn {
      width: 100%;
      max-width: 300px;
    }
  }

  @media (max-width: 480px) {
    .impact-grid {
      grid-template-columns: 1fr;
    }

    .overview-stats {
      grid-template-columns: 1fr;
    }
  }
</style>
