---
import BaseLayout from '../layouts/BaseLayout.astro';
import QuoteBlock from '../components/QuoteBlock.astro';

let basePath =
  import.meta.env.BASE_URL != null && import.meta.env.BASE_URL !== ''
    ? import.meta.env.BASE_URL
    : '/testigos-solarpunk';

// Ensure basePath doesn't end with a slash
if (basePath.endsWith('/')) {
  basePath = basePath.slice(0, -1);
}
---

<BaseLayout title="Inicio">
  <section class="hero gradient-solarpunk">
    <div class="container">
      <div class="hero-content">
        <div class="hero-logo-title animate-fade-in">
          <img
            src={`${basePath}/images/logo.png`}
            alt="Testigos de Solarpunk"
            class="hero-logo"
          />
          <h1 class="hero-title">Testigos de Solarpunk</h1>
        </div>
        <p class="hero-subtitle animate-fade-in">
          Conectar, Concientizar y Crear Comunidad a través del humor sostenible
        </p>
        <div class="hero-actions animate-fade-in">
          <a href={`${basePath}/contenido/sketches/`} class="btn btn-primary"
            ><span class="emoji-cutout emoji-button emoji-md theme-solar"
              >🎬</span
            > Ver Sketches</a
          >
          <a href={`${basePath}/proyecto/`} class="btn btn-secondary"
            >Conocer el Proyecto</a
          >
        </div>
      </div>
    </div>
  </section>

  <section class="fases-home">
    <div class="container">
      <h2 class="text-center">Nuestra Estrategia de Impacto</h2>
      <div class="grid grid-3">
        <div class="fase-card-home" data-fase="1">
          <div class="fase-card-content">
            <span class="fase-icon"
              ><span
                class="emoji-cutout emoji-card emoji-xl theme-solar flutter"
                >🎭</span
              ></span
            >
            <h3>Fase 1: Atracción</h3>
            <p>
              Sketches de comedia viral que rompen barreras culturales sobre
              sostenibilidad
            </p>
          </div>
          <div class="fase-card-footer">
            <span class="fase-status active">En Producción</span>
            <a href={`${basePath}/contenido/sketches/`} class="btn btn-primary"
              >Ver Sketches</a
            >
          </div>
        </div>

        <div class="fase-card-home" data-fase="2">
          <div class="fase-card-content">
            <span class="fase-icon"
              ><span
                class="emoji-cutout emoji-card emoji-xl theme-purple flutter"
                >🎙️</span
              ></span
            >
            <h3>Fase 2: Conexión</h3>
            <p>
              Podcast con expertos para profundizar en soluciones prácticas y
              escalables
            </p>
          </div>
          <div class="fase-card-footer">
            <span class="fase-status upcoming">Próximamente</span>
            <a href={`${basePath}/contenido/podcast/`} class="btn btn-secondary"
              >Saber Más</a
            >
          </div>
        </div>

        <div class="fase-card-home" data-fase="3">
          <div class="fase-card-content">
            <span class="fase-icon"
              ><span
                class="emoji-cutout emoji-card emoji-xl theme-green flutter"
                >🔬</span
              ></span
            >
            <h3>Fase 3: Conversión</h3>
            <p>
              MADLAB: Eventos y experiencias educativas que transforman
              comunidades escolares
            </p>
          </div>
          <div class="fase-card-footer">
            <span class="fase-status development">En Desarrollo</span>
            <a href={`${basePath}/contenido/madlab/`} class="btn btn-secondary"
              >Descubrir</a
            >
          </div>
        </div>
      </div>
    </div>
  </section>

  <section class="evangelista-section">
    <div class="container">
      <h2 class="text-center evangelista-title">El Evangelio Solar</h2>
      <QuoteBlock
        quote="Hermanos, el Sol nos ama y quiere vernos iluminados. Cada panel es una oración al astro rey."
        author="Hermana Panelia"
        variant="solar"
      />
      <div class="evangelista-grid">
        <div class="evangelista-card halo-effect">
          <h3>
            <span class="emoji-cutout emoji-card emoji-lg theme-solar">🌟</span>
            Los Evangelistas
          </h3>
          <p>
            Tres misioneros dedicados a predicar la salvación ecológica y la
            redención energética de la humanidad.
          </p>
          <a href={`${basePath}/personajes/`} class="btn btn-primary"
            >Conocer Evangelistas</a
          >
        </div>
        <div class="evangelista-card halo-effect">
          <h3>
            <span class="emoji-cutout emoji-card emoji-lg theme-solar">📖</span>
            El Mensaje
          </h3>
          <p>
            Transformación espiritual a través de la tecnología sostenible y la
            reconexión con los ciclos naturales.
          </p>
          <a href={`${basePath}/filosofia/`} class="btn btn-primary"
            >Leer Filosofía</a
          >
        </div>
        <div class="evangelista-card halo-effect">
          <h3>
            <span class="emoji-cutout emoji-card emoji-lg theme-solar">🎬</span>
            Los Formatos
          </h3>
          <p>
            Múltiples maneras de llevar la luz solar a cada hogar: videos,
            podcasts, sketches y más.
          </p>
          <a href={`${basePath}/formatos/`} class="btn btn-primary"
            >Ver Formatos</a
          >
        </div>
      </div>
    </div>
  </section>

  <section class="vision-section">
    <div class="container">
      <h2 class="text-center">Nuestra Visión</h2>
      <div class="vision-grid">
        <div class="vision-item float-animation">
          <span class="vision-icon"
            ><span
              class="emoji-cutout emoji-hero emoji-xl theme-solar interactive"
              >☀️</span
            ></span
          >
          <h4>Energía Limpia</h4>
          <p>
            Un mundo impulsado por el sol, el viento y la creatividad humana
          </p>
        </div>
        <div class="vision-item float-animation" style="animation-delay: 0.2s">
          <span class="vision-icon"
            ><span
              class="emoji-cutout emoji-hero emoji-xl theme-community interactive"
              >🤝</span
            ></span
          >
          <h4>Comunidad Fuerte</h4>
          <p>Las soluciones nacen de la colaboración y el apoyo mutuo</p>
        </div>
        <div class="vision-item float-animation" style="animation-delay: 0.4s">
          <span class="vision-icon"
            ><span
              class="emoji-cutout emoji-hero emoji-xl theme-green interactive"
              >🌺</span
            ></span
          >
          <h4>Armonía Natural</h4>
          <p>La tecnología y la naturaleza coexisten en equilibrio</p>
        </div>
        <div class="vision-item float-animation" style="animation-delay: 0.6s">
          <span class="vision-icon"
            ><span
              class="emoji-cutout emoji-hero emoji-xl theme-solar interactive"
              >🎭</span
            ></span
          >
          <h4>Historias que Inspiran</h4>
          <p>Narrativas que muestran futuros posibles y deseables</p>
        </div>
      </div>
    </div>
  </section>

  <section class="cta-section">
    <div class="container text-center">
      <h2>¿Listo para cambiar el mundo con humor?</h2>
      <p class="text-muted mb-3">
        Únete a los Testigos de Solarpunk y sé parte del movimiento
      </p>
      <div class="cta-actions">
        <a href={`${basePath}/comunidad/`} class="btn btn-primary"
          ><span class="emoji-cutout emoji-button emoji-md theme-community"
            >🤝</span
          > Únete a la Comunidad</a
        >
        <a href={`${basePath}/impacto/`} class="btn btn-secondary"
          ><span class="emoji-cutout emoji-button emoji-md theme-green">📊</span
          > Ver Nuestro Impacto</a
        >
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  .hero {
    padding: var(--espacio-xxl) 0;
    text-align: center;
    color: white;
    position: relative;
    overflow: hidden;
  }

  .hero::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: rgba(0, 0, 0, 0.2);
    z-index: 1;
  }

  .hero-content {
    position: relative;
    z-index: 2;
  }

  .hero-logo-title {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--espacio-md);
    margin-bottom: var(--espacio-lg);
  }

  .hero-logo {
    height: 120px;
    width: auto;
    filter: drop-shadow(0 4px 8px rgba(0, 0, 0, 0.3));
  }

  .hero-title {
    font-size: 3.5rem;
    margin: 0;
    color: white;
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
  }

  .hero-subtitle {
    font-size: 1.5rem;
    margin-bottom: var(--espacio-xl);
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
  }

  .hero-actions {
    display: flex;
    gap: var(--espacio-md);
    justify-content: center;
  }

  .evangelista-section {
    padding: var(--espacio-xxl) 0;
    background: linear-gradient(
      135deg,
      var(--fondo-primario) 0%,
      rgba(255, 215, 0, 0.05) 100%
    );
  }

  .evangelista-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: var(--espacio-xl);
    margin-top: var(--espacio-xl);
  }

  .evangelista-card {
    background: var(--bg-secondary, #1a1a1b);
    color: var(--text-primary, #ffffff);
    padding: var(--espacio-xl);
    border-radius: 12px;
    text-align: center;
    border: 1px solid var(--border-subtle, rgba(255, 255, 255, 0.1));
    transition: all var(--transicion-normal);
  }

  .evangelista-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 12px 24px rgba(0, 0, 0, 0.4);
    border-color: var(--dorado-solar);
  }

  .evangelista-card h3 {
    color: var(--dorado-solar);
    margin-bottom: var(--espacio-md);
  }

  .evangelista-card .btn {
    margin-top: var(--espacio-md);
  }

  .vision-section {
    background-color: var(--fondo-secundario);
    padding: var(--espacio-xxl) 0;
    margin-top: 0;
  }

  .vision-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: var(--espacio-xl);
    margin-top: var(--espacio-xl);
  }

  .vision-item {
    text-align: center;
  }

  .vision-icon {
    font-size: 3rem;
    display: block;
    margin-bottom: var(--espacio-md);
  }

  .cta-section {
    padding: var(--espacio-xxl) 0;
  }

  .cta-actions {
    display: flex;
    gap: var(--espacio-md);
    justify-content: center;
  }

  /* Fases Home Section */
  .fases-home {
    padding: var(--espacio-xxl) 0;
    background: var(--fondo-secundario);
  }

  .fases-home h2 {
    font-size: 2.5rem;
    margin-bottom: var(--espacio-xl);
  }

  .fase-card-home {
    background: #1a1a1b; /* Dark background directly */
    padding: var(--espacio-xl);
    border-radius: 16px;
    text-align: center;
    position: relative;
    overflow: hidden;
    transition: all 0.3s ease;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.3);
    border: 1px solid rgba(255, 255, 255, 0.1);
    color: #ffffff; /* White text directly */
    display: flex;
    flex-direction: column;
    min-height: 350px;
  }

  .fase-card-home h3 {
    color: #ffffff;
  }

  .fase-card-home p {
    color: #cccccc;
  }

  .fase-card-content {
    flex: 1;
    display: flex;
    flex-direction: column;
  }

  .fase-card-footer {
    display: flex;
    flex-direction: column;
    gap: var(--espacio-md);
    align-items: center;
    margin-top: auto;
    padding-top: var(--espacio-md);
  }

  .fase-card-home::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 4px;
    background: var(--amarillo-solar);
  }

  .fase-card-home[data-fase='1']::before {
    background: var(--amarillo-solar);
  }

  .fase-card-home[data-fase='2']::before {
    background: var(--purpura-creativo);
  }

  .fase-card-home[data-fase='3']::before {
    background: var(--verde-sostenible);
  }

  .fase-card-home:hover {
    transform: translateY(-5px);
    box-shadow: 0 12px 24px rgba(0, 0, 0, 0.4);
    border-color: var(--amarillo-solar);
  }

  .fase-icon {
    font-size: 3rem;
    display: block;
    margin-bottom: var(--espacio-md);
  }

  .fase-status {
    display: inline-block;
    padding: 4px 12px;
    border-radius: 20px;
    font-size: 0.875rem;
    font-weight: 500;
  }

  .fase-status.active {
    background: var(--verde-sostenible);
    color: white;
  }

  .fase-status.upcoming {
    background: var(--azul-tecnologico);
    color: white;
  }

  .fase-status.development {
    background: var(--naranja-comunidad);
    color: white;
  }

  @media (max-width: 768px) {
    .hero-logo {
      height: 80px;
    }

    .hero-title {
      font-size: 2.5rem;
    }

    .hero-subtitle {
      font-size: 1.25rem;
    }

    .hero-actions,
    .cta-actions {
      flex-direction: column;
      align-items: center;
    }
  }
</style>
