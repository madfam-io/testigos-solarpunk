---
import BaseLayout from '../../layouts/BaseLayout.astro';

let basePath =
  import.meta.env.BASE_URL != null && import.meta.env.BASE_URL !== ''
    ? import.meta.env.BASE_URL
    : '/testigos-solarpunk';

// Ensure basePath doesn't end with a slash
if (basePath.endsWith('/')) {
  basePath = basePath.slice(0, -1);
}
---

<BaseLayout title="Producción">
  <section class="hero-section">
    <div class="container">
      <h1 class="text-gradient">Guías de Producción</h1>
      <p class="lead">
        Todo lo que necesitas para crear contenido en el universo de Testigos de
        Solarpunk
      </p>
    </div>
  </section>

  <section class="guides-section">
    <div class="container">
      <div class="grid grid-2">
        <div class="guide-card card">
          <h2>
            <span class="emoji emoji-md emoji-card">🎨</span> Guía de Estilo Visual
          </h2>
          <p>
            Paletas de color, tipografías, y referencias visuales para mantener
            consistencia estética.
          </p>
          <ul>
            <li>Paleta de colores MADFAM + Solarpunk</li>
            <li>Referencias de vestuario</li>
            <li>Mood boards por ubicación</li>
            <li>Guías de iluminación</li>
          </ul>
          <a
            href={`${basePath}/produccion/estilo-visual/`}
            class="btn btn-primary mt-3">Ver Guía Completa</a
          >
        </div>

        <div class="guide-card card">
          <h2>
            <span class="emoji emoji-md emoji-card">🎭</span> Tono y Voz Narrativa
          </h2>
          <p>
            Cómo contar historias que inspiren esperanza y acción en el mundo
            solarpunk.
          </p>
          <ul>
            <li>Principios narrativos</li>
            <li>Arquetipos de personajes</li>
            <li>Temas recurrentes</li>
            <li>Ejemplos de diálogos</li>
          </ul>
          <a
            href={`${basePath}/produccion/tono-voz/`}
            class="btn btn-primary mt-3">Explorar Guía</a
          >
        </div>

        <div class="guide-card card">
          <h2>
            <span class="emoji emoji-md emoji-card">🎬</span> Especificaciones Técnicas
          </h2>
          <p>
            Requerimientos técnicos para cada plataforma y formato de contenido.
          </p>
          <ul>
            <li>Formatos de video por plataforma</li>
            <li>Configuraciones de audio</li>
            <li>Subtítulos y accesibilidad</li>
            <li>Exportación y entrega</li>
          </ul>
          <a href={`${basePath}/produccion/specs/`} class="btn btn-primary mt-3"
            >Ver Especificaciones</a
          >
        </div>

        <div class="guide-card card">
          <h2>
            <span class="emoji emoji-md emoji-card">🎵</span> Música y Diseño Sonoro
          </h2>
          <p>
            Biblioteca de audio y guías para crear paisajes sonoros solarpunk.
          </p>
          <ul>
            <li>Temas musicales principales</li>
            <li>Efectos de sonido ambiente</li>
            <li>Leitmotifs de personajes</li>
            <li>Licencias y atribución</li>
          </ul>
          <a href={`${basePath}/produccion/audio/`} class="btn btn-primary mt-3"
            >Escuchar Biblioteca</a
          >
        </div>
      </div>
    </div>
  </section>

  <section class="brand-section">
    <div class="container text-center">
      <h2>Lineamientos de Marca</h2>
      <p class="text-muted mb-4">
        Mantén la coherencia visual y narrativa del universo Testigos de
        Solarpunk
      </p>
      <div class="brand-downloads">
        <a
          href={`${basePath}/downloads/logo-pack.zip`}
          class="btn btn-secondary"
          ><span class="emoji emoji-button">📦</span> Pack de Logos</a
        >
        <a
          href={`${basePath}/downloads/brand-guide.pdf`}
          class="btn btn-secondary"
          ><span class="emoji emoji-button">📖</span> Manual de Marca</a
        >
        <a
          href={`${basePath}/downloads/templates.zip`}
          class="btn btn-secondary"
          ><span class="emoji emoji-button">🎨</span> Plantillas</a
        >
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  .hero-section {
    background-color: var(--fondo-secundario);
    padding: var(--espacio-xxl) 0;
    text-align: center;
  }

  .lead {
    font-size: 1.25rem;
    max-width: 700px;
    margin: 0 auto;
    color: var(--texto-secundario);
  }

  .guides-section {
    padding: var(--espacio-xxl) 0;
  }

  .guide-card h2 {
    color: var(--verde-sostenible);
    margin-bottom: var(--espacio-md);
  }

  .guide-card ul {
    list-style: none;
    padding: 0;
    margin: var(--espacio-md) 0;
  }

  .guide-card li {
    padding: var(--espacio-sm) 0;
    padding-left: var(--espacio-lg);
    position: relative;
    color: var(--texto-secundario);
  }

  .guide-card li::before {
    content: '→';
    position: absolute;
    left: 0;
    color: var(--amarillo-solar);
  }

  .brand-section {
    background-color: var(--fondo-secundario);
    padding: var(--espacio-xxl) 0;
  }

  .brand-downloads {
    display: flex;
    gap: var(--espacio-md);
    justify-content: center;
    flex-wrap: wrap;
  }

  @media (max-width: 768px) {
    .brand-downloads {
      flex-direction: column;
      align-items: center;
    }

    .brand-downloads .btn {
      width: 250px;
    }
  }
</style>
